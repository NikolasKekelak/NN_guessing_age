#include "MATRIX.h"

using namespace std;



class NNN{
      int numb_of_inputs;
      int numb_of_layers;
      int numb_of_outputs;
      vector<MATRIX<double>> LAYERS ;
      vector<double> OUTPUT;

      NNN(int inputs, int layers, int outputs)
      : numb_of_inputs(inputs), numb_of_layers(layers), numb_of_outputs(){};
};


int main(){


  return 0;
}